name: preventra
description: "A new Flutter project."
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.0.0 # Saya sesuaikan ke range aman agar kompatibel

dependencies:
  flutter:
    sdk: flutter

  encrypt: ^5.0.3
  file_picker: ^8.0.0

  # --- CORE ---
  provider: ^6.1.2
  get_it: ^7.7.0
  equatable: ^2.0.5
  intl: ^0.19.0

  # --- NETWORK & API (Modul A) ---
  dio: ^5.4.3
  #workmanager: ^0.5.2
  flutter_local_notifications: ^17.1.0

  # --- DATABASE & SECURITY (Modul B & D) ---
  sqflite_sqlcipher: ^3.1.0+1
  path_provider: ^2.1.3
  flutter_secure_storage: ^9.2.2
  local_auth: ^2.2.0
  crypto: ^3.0.3
  #flutter_jailbreak_detection: ^1.0.0
  
  # --- UI & UX (Modul C) ---
  google_fonts: ^6.2.1
  lottie: ^3.1.2
  fl_chart: ^0.68.0
  flutter_animate: ^4.5.0

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  # Linting rules
  flutter_lints: ^5.0.0
  
  # --- TAMBAHAN WAJIB (INI YANG TADINYA HILANG) ---
  flutter_launcher_icons: ^0.13.1

flutter:
  uses-material-design: true

  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

# --- KONFIGURASI ICON ---
flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/icon.png"
  min_sdk_android: 23